DROP TABLE IF EXISTS dog CASCADE;
CREATE TABLE dog (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY,
    age INTEGER NOT NULL CHECK (age>=0 AND age<=36),
    breed VARCHAR(50), name VARCHAR(50),
    PRIMARY KEY (id)
);